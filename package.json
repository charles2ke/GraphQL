{
  "name": "graphql-demo-service",
  "version": "1.0.0",
  "description": "A minimal backend service exposing a GraphQL API (users and posts) with in-memory storage.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "keywords": [
    "graphql",
    "apollo-server",
    "express",
    "microservice"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "@apollo/server": "^5.5.1",
    "@as-integrations/express5": "^1.1.2",
    "@graphql-tools/schema": "^10.1.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "graphql": "^16.14.2"
  }
}
